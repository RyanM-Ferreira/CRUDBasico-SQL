{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Iniciar Node App (local)",
            "program": "${workspaceFolder}/app.js",
            "args": [
                "--local"
            ]
        }
    ]
}